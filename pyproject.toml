[project]
name = "toy-gatekeeper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "DaytonFishell", email = "d_fishell@icloud.com" }
]
requires-python = ">=3.11"
dependencies = [
    "cryptography>=41.0.0",
    "pyinstaller>=6.0.0",
]

[project.scripts]
toy-gatekeeper = "toy_gatekeeper:main"
toy-protector = "toy_protector:main"

[build-system]
requires = ["uv_build>=0.11.11,<0.12.0"]
build-backend = "uv_build"
